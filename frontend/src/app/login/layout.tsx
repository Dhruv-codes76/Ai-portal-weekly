export default function LoginLayout({ children }: { children: React.ReactNode }) {
    return <div className="h-screen w-full bg-[#0E0E10] overflow-hidden m-0 p-0 text-white font-sans">{children}</div>;
}
